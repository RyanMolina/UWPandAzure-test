<UserControl
    x:Class="App.Control.ResultController"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App.Control"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <RelativePanel>
        <TextBlock Name="result" FontSize="18" Text="{x:Bind SearchResult.Result}" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignTopWithPanel="True" Margin="10"/>
        <TextBlock Text="{x:Bind SearchResult.Address}" RelativePanel.Below="result" Margin="10" />
        <TextBlock Text="{x:Bind SearchResult.Type}" RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True" />
    </RelativePanel>
</UserControl>
